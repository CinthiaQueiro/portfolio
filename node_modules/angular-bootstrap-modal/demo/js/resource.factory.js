(function() {
    'use strict';

    angular
        .module('app')
        .factory('App', App);

    App.$inject = ['$resource'];

    /* @ngInject */
    function App($resource) {
        return $resource('server/sample.php');
    }
})();
